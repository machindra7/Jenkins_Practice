library identifier: 'shared_libs_in_pipeline@main',
        retriever: modernSCM([$class: 'GitSCMSource', remote: 'https://github.com/machindra7/shared_libs_in_pipeline.git'])

pipeline {
    agent any
    stages {
        stage('Audit tools') {
            steps {
                auditTools()
            }
        }
    }
}
